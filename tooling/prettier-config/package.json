{
  "name": "@repo/prettier-config",
  "version": "0.1.0",
  "private": true,
  "main": "index.json",
  "files": [
    "index.json"
  ],
  "scripts": {
    "build": "echo 'Config package - no build required'",
    "types": "echo 'Config package - no types to check'",
    "test": "echo 'Config package - no tests'"
  }
}
