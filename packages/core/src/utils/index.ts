/**
 * @module utils
 * @description Utility exports for the core package.
 *
 * Color utilities have been moved to individual packages that need them.
 * Core is now pure and browser-compatible.
 */

/**
 * Marker constant indicating that color utilities have been relocated.
 * Exists to satisfy linter requirements for non-empty module files.
 *
 * @constant
 * @since 0.1.0
 */
export const CORE_UTILS_MOVED = true
