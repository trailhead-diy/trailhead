import { createCommand } from '@trailhead/cli/command' import { ok } from '@trailhead/core'
export const helpCommand = createCommand({ name: 'help', description: 'Show help information',
action: async (options, context) => { const { logger } = context logger.info(`
{{projectName}}
{{description}}

Usage:
{{packageName}}
<command />
[options] Commands: help Show this help message version Show version information
{{#if features.config}}
  config Manage configuration
{{/if}}

Options: -h, --help Show help -v, --version Show version --verbose Enable verbose output For more
information about a specific command:
{{packageName}}
<command />
--help `) return ok(undefined) } })