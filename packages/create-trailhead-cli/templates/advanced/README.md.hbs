# {{projectName}}

{{description}}

Built with [@esteban-url/trailhead-cli](https://github.com/esteban-url/trailhead) framework.

## Features

This advanced template includes:

- 🏗️ **Multiple command examples** - Build, dev, init, config, validate
- ⚙️ **Configuration management** - Type-safe config with Zod validation
- 🔍 **Validation utilities** - Project validation with auto-fix capabilities
- ⚡ **Performance optimization** - Built-in performance tracking and caching
- 🧪 **Comprehensive testing** - Unit and integration tests with high ROI approach
- 📊 **Advanced TypeScript** - Strict types, decorators, and modern patterns

## Installation

```bash
# Install globally
{{packageManager}} add -g {{packageName}}

# Or run directly
npx {{packageName}}
```

## Usage

### Core Commands

```bash
# Build the project
{{packageName}} build --output dist --watch

# Start development server  
{{packageName}} dev --port 3000 --open

# Initialize new project configuration
{{packageName}} init --template library

# Manage configuration
{{packageName}} config --list
{{packageName}} config build.target
{{packageName}} config --set build.minify=true

# Validate project files
{{packageName}} validate --source --tests --fix
```

## Commands Reference

### `build`
Build the project with advanced options.

```bash
{{packageName}} build [options]

Options:
  -o, --output <dir>    Output directory (default: dist)
  -w, --watch          Watch for changes
  --minify             Minify output
  --sourcemap          Generate sourcemaps
```

### `dev`
Start development server with hot reload.

```bash
{{packageName}} dev [options]

Options:
  -p, --port <number>   Port to serve on (default: 3000)
  -h, --host <string>   Host to bind to (default: localhost)
  --open               Open browser automatically
  -i, --interactive    Run in interactive mode
```

### `init`
Initialize project configuration interactively.

```bash
{{packageName}} init [options]

Options:
  -f, --force          Overwrite existing configuration
  -t, --template <type> Project template (library|application|cli|api)
  --skip-install       Skip dependency installation
```

### `config`
Manage project configuration with type safety.

```bash
{{packageName}} config [key] [value]
{{packageName}} config --get <key>
{{packageName}} config --set <key=value>
{{packageName}} config --list
{{packageName}} config --validate
```

### `validate`
Validate project files with auto-fix capabilities.

```bash
{{packageName}} validate [options]

Options:
  -c, --config         Validate configuration files only
  -s, --source         Validate source code files
  -t, --tests          Validate test files
  -f, --fix            Auto-fix issues where possible
  -p, --pattern <glob>  Custom file pattern to validate
```

## Development

```bash
# Install dependencies
{{packageManager}} install

# Start development
{{packageManager}} dev

# Run tests
{{packageManager}} test

# Run tests in watch mode
{{packageManager}} test:watch

# Generate coverage report
{{packageManager}} test:coverage

# Validate project
{{packageManager}} validate

# Build for production
{{packageManager}} build
```

## Architecture

This template demonstrates advanced CLI patterns:

### Performance Optimization
- **Performance tracking** with decorators and metrics
- **Caching layer** for expensive operations
- **Batch processing** for efficient bulk operations

### Type Safety
- **Strict TypeScript** configuration
- **Zod schemas** for runtime validation
- **Event system** with typed payloads

### Testing Strategy
- **High-ROI tests** focusing on business logic
- **Integration tests** for command workflows
- **Mock utilities** from trailhead-cli/testing

### Project Structure

```
{{projectName}}/
├── src/
│   ├── index.ts          # Main CLI entry point
│   ├── commands/         # All CLI commands
│   │   ├── build.ts      # Build command
│   │   ├── dev.ts        # Development server
│   │   ├── init.ts       # Project initialization
│   │   ├── config.ts     # Configuration management
│   │   └── validate.ts   # Project validation
│   ├── lib/              # Shared utilities
│   │   └── performance.ts # Performance utilities
│   └── types/            # TypeScript definitions
│       └── index.ts      # Centralized types
├── tests/
│   ├── unit/             # Unit tests for commands
│   ├── integration/      # End-to-end CLI tests
│   └── fixtures/         # Test data and mocks
├── examples/             # Usage examples
├── docs/                 # Project documentation
└── dist/                 # Built output
```

## Configuration

The project uses a type-safe configuration system:

```typescript
// {{packageName}}.config.js
export default {
  name: "{{projectName}}",
  type: "cli",
  version: "{{version}}",
  features: {
    linting: true,
    testing: true,
    typescript: true
  },
  build: {
    outDir: "dist",
    target: "node18",
    sourcemap: true,
    minify: false
  }
}
```

## Performance Features

### Automatic Performance Tracking
```typescript
import { trackPerformance } from './lib/performance.js'

class MyService {
  @trackPerformance('expensive-operation')
  async processData(data: any[]) {
    // Performance automatically tracked
  }
}
```

### Built-in Caching
```typescript
import { SimpleCache } from './lib/performance.js'

const cache = new SimpleCache<string>(300000) // 5 min TTL
cache.set('key', 'value')
const value = cache.get('key')
```

## Documentation

For more information about the trailhead-cli framework, visit the [documentation](https://github.com/esteban-url/trailhead).

## Contributing

1. Fork the repository
2. Create your feature branch (`git checkout -b feature/amazing-feature`)
3. Commit your changes (`git commit -m 'feat: add amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

## License

{{license}} © {{currentYear}} {{author}}