/**
 * Prettier configuration for the Trailhead monorepo
 *
 * This configuration extends the shared prettier settings from @repo/prettier-config
 * and serves as the root configuration for all packages in the monorepo.
 */

module.exports = require('./tooling/prettier-config/index.js')
