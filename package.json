{
  "name": "trailhead",
  "version": "1.0.0",
  "private": true,
  "description": "Trailhead is a toolkit for building UI libraries and CLIs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/esteban-url/trailhead.git"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "test:coverage": "turbo test:coverage",
    "coverage": "turbo test:coverage",
    "coverage:changed": "turbo test:coverage --affected",
    "coverage:report": "pnpm dev-cli coverage-check",
    "lint": "oxlint . --config .oxlintrc.json",
    "lint:fix": "oxlint . --fix --config .oxlintrc.json",
    "types": "turbo types --filter=./packages/*",
    "clean": "turbo clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --log-level=error --ignore-path .gitignore --ignore-pattern \"**/*.hbs\" ",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\" --log-level=error --ignore-path .gitignore --ignore-pattern \"**/*.hbs\"",
    "ci:run": "pnpm dev-cli ci",
    "ci:local": "pnpm dev-cli ci",
    "ci:affected": "pnpm dev-cli ci --affected",
    "ci:coverage": "pnpm dev-cli ci --coverage",
    "ci:quick": "turbo lint types test --affected --cache-dir=.turbo --concurrency=100%",
    "ci:watch": "turbo watch lint types test",
    "ci:fresh": "rm -rf .turbo node_modules/.cache && pnpm test",
    "ci:validate": "pnpm ci:fresh && echo '✅ Clean build passes'",
    "push:coverage": "RUN_COVERAGE=1 git push",
    "health:deps": "turbo run build --dry-run --graph",
    "health:cache": "turbo run build --summarize",
    "health:fresh": "rm -rf .turbo && pnpm test",
    "health:validate": "pnpm dev-cli validate-deps",
    "health:all": "pnpm health:validate && pnpm health:deps && pnpm health:cache",
    "docs:validate": "tsx tooling/docs-tooling/src/validate-docs.ts",
    "docs:validate-code": "pnpm dev-cli validate-docs --pattern '{docs,packages}/**/*.{md,mdx}' --strict",
    "docs:validate-all": "pnpm docs:validate && pnpm docs:validate-code",
    "docs:lint": "vale docs && markdownlint docs/**/*.md",
    "docs:coverage": "tsx tooling/docs-tooling/src/validate-docs.ts --coverage",
    "docs:new": "tsx tooling/docs-tooling/src/create-doc.ts",
    "docs:validate-links": "tsx tooling/docs-tooling/src/validate-links.ts",
    "docs:fix-links": "pnpm dev-cli fix-links",
    "docs:check-syntax": "pnpm dev-cli check-syntax",
    "docs:api": "pnpm dev-cli generate-api",
    "docs:api:clean": "pnpm dev-cli generate-api --clean",
    "docs:api:watch": "pnpm dev-cli generate-api --watch",
    "docs:api:validate": "pnpm dev-cli generate-api && echo '✅ API docs generated'",
    "docs:api:dev": "pnpm dev-cli generate-api --clean --watch",
    "predocs:build": "pnpm docs:api && pnpm docs:validate-code",
    "predocs:dev": "pnpm docs:api",
    "changeset": "changeset",
    "changeset:add": "changeset add",
    "changeset:status": "changeset status --verbose",
    "version-packages": "changeset version",
    "release": "turbo build --filter=./packages/* && changeset publish",
    "release:dry-run": "changeset publish --dry-run",
    "fresh-start": "pnpm dev-cli fresh-start",
    "create-cli": "node packages/create-cli/bin/cli.js",
    "dev:demos": "turbo dev --filter=./apps/demos/*",
    "build:packages": "turbo build --filter=./packages/*",
    "validate:deps": "pnpm dev-cli validate-interdeps",
    "prepare": "lefthook install",
    "lefthook": "lefthook",
    "lefthook:install": "lefthook install",
    "lefthook:uninstall": "lefthook uninstall",
    "docs:dev": "turbo dev --filter=@repo/docs",
    "docs:build": "turbo build --filter=@repo/docs",
    "docs:serve": "turbo serve --filter=@repo/docs"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.5",
    "@inquirer/prompts": "^7.8.0",
    "@repo/dev-cli": "workspace:*",
    "@types/node": "^24.1.0",
    "chalk": "^5.4.1",
    "gray-matter": "^4.0.3",
    "lefthook": "^1.12.2",
    "oxlint": "^1.9.0",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.7.0",
    "tsx": "^4.20.3",
    "turbo": "^2.5.5",
    "typedoc": "^0.28.9",
    "typedoc-plugin-frontmatter": "^1.3.0",
    "typedoc-plugin-markdown": "^4.8.0",
    "typescript": "^5.8.3",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "overrides": {
      "glob@<10": ">=11.0.0",
      "inflight": "npm:@void/noop@^1.0.0"
    },
    "onlyBuiltDependencies": [
      "@tailwindcss/oxide",
      "core-js",
      "core-js-pure",
      "esbuild",
      "lefthook"
    ]
  }
}
